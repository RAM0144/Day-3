//Convert all the strings to title caps in a string array
    
function str(animals) {
  let str=[];
  for (let.caps(animals) in str) {
    if(str.indexOf(animals).split(",").join(",").toLowerCase) {    
        str.push(animals);
    }
}
}
const str ="tiger,lion,elephant";
let str = str(animals);
console.log(str);